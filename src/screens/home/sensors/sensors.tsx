import React from "react";
import { View } from "react-native";

/**
 * Unified button base on react-native-elements button.
 * Unify colors and styles.
 */
export default function Sensors(props: {}): JSX.Element {
  return <View />;
}
